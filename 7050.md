# 头条号 7050 保存失败排查摘要

更新时间：2026-06-24
项目目录：`/root/cc/autowz`
相关文件：`app/services/publish/channels/toutiao.py`

## 1. 目标

autowz 定时生成文章后，保存到头条号**云端草稿箱**，方便后续在手机端/PC 端人工发布。

## 2. 当前现象

最近自动生成的文章可以成功保存到：

- 微信草稿
- 百家号草稿

但头条号失败：

```json
{"code":7050,"err_no":7050,"message":"保存失败","reason":"保存失败","data":{"pgc_id":"0"}}
```

失败文章示例：

- `article_id=341`：`会“呼吸”的菜场，凭什么出圈`
- `article_id=342`：`小米跌到22港元，网红一句话惹火股民`

本地 `articles.status` 可能仍是 `draft_saved`，因为代码逻辑是“任一渠道成功即 draft_saved”；头条单渠道失败要看 `wechat_publish_records.publish_status=publish_failed`。

## 3. 账号与登录态

服务器当前登录态文件：

```text
app/services/publish/cookies/toutiao_state.json
```

确认是同一个头条号：

```text
user_id: 3898830537
media_id: 1868415537402892
账号名: 不爱玩29276399
```

用户用 PC 浏览器扫码登录同一账号。

## 4. 云端草稿验证

用户开启“草稿保存到云端”后，在 PC 网页端创建了标题和正文均为 `yun` 的草稿。

服务器只读验证成功：

```text
草稿箱 URL: https://mp.toutiao.com/profile_v4/manage/draft
标题: yun
草稿 ID: 7654897374749377070
内容: <p data-track="1">yun</p>
```

接口：

```text
GET /mp/agw/creator_center/draft_list?type=0&count=50&app_id=1231
```

返回能看到 `yun`。

草稿详情接口：

```text
GET /mp/agw/article/edit?pgc_id=7654897374749377070&wxstyle=0&format=json
```

关键字段：

```json
{
  "title": "yun",
  "content": "<p data-track=\"1\">yun</p>",
  "is_draft": true,
  "article_ad_type": 3,
  "draft_form_data": "{\"coverType\":2}",
  "pgc_feed_covers": [],
  "content_source": 100000000402,
  "content_channel_source": 54,
  "device_platform": "pc",
  "soft_type": "Chrome 148.0.0.0",
  "mp_editor_stat": "{}",
  "from_caller": "toutiao.content.publish",
  "ad_type": 3,
  "show_ads": 3,
  "tuwen_wtt_transfer_switch": "1"
}
```

结论：账号本身能保存云端草稿，且 `mp_editor_stat={}` 也能成功，不是这个字段导致 7050。

## 5. 已尝试方案与结果

### 5.1 直接调用头条保存接口

当前代码路径：`ToutiaoChannel._call_publish_api()`

接口：

```text
POST /mp/agw/article/publish?source=mp&type=article&aid=1231&mp_publish_ab_val=0
```

草稿参数：

```text
save=0
```

注意：历史已确认头条字段语义是：

```text
save=0 => 保存草稿
save=1 => 发表/直接发布
```

不要随便改回 `save=1`，有误发风险。

直接 API 测试标题 `AutoYunTest`，结果仍然：

```json
err_no=7050 保存失败
```

### 5.2 修改 article_ad_type

PC 成功草稿显示：

```text
article_ad_type=3
```

原代码硬编码：

```js
fd.append('article_ad_type', '2')
```

临时改为从：

```text
/mp/agw/article/new?article_type=0&format=json&compat=1&column_no=
```

读取默认 `article_ad_type`，兜底 `3`。

结果：仍然 7050。

说明：`article_ad_type` 不是唯一原因。

### 5.3 模拟 PC 页面自动保存链路

用 Playwright 打开：

```text
https://mp.toutiao.com/profile_v4/graphic/publish
```

定位标题 textarea 和正文 `.ProseMirror`。

已确认通过坐标点击能把正文输入进编辑器：

```html
<p data-track="1">CoordCloud</p>
```

页面自身触发了官方自动保存请求，URL 中带 `msToken` 和 `a_bogus`：

```text
/mp/agw/article/publish?...&msToken=...&a_bogus=...
```

但结果仍然：

```json
err_no=7050 保存失败
```

说明：不是缺 `msToken/a_bogus`，也不是正文没进编辑器。

## 6. 当前最可能原因

真实 PC 浏览器可以保存云端草稿；服务器 headless Playwright 即使走页面自身自动保存链路，也返回 7050。

因此当前最可能是：

> 服务器 headless Playwright 环境触发头条保存风控或环境校验，导致保存接口拒绝。

可疑点：

- Playwright headless UA：`HeadlessChrome/...`
- `navigator.webdriver=true`
- Linux 服务器环境
- 可能缺真实浏览器环境指纹
- 可能需要 headful / xvfb / stealth / 真实 Chrome profile / 用户当前 PC 导出的更完整状态

但历史中也尝试过简单隐藏 webdriver，未必够。

## 7. 当前工作区改动

用户要求先不提交 Git。

当前已有未提交改动：

```text
 M app/services/wechat/cover_generator.py
 M image_providers.json
?? image_providers.json.bak.220830
```

本轮排查中还临时修改了：

```text
app/services/publish/channels/toutiao.py
```

改动内容：读取默认 `article_ad_type`，不再硬编码 2。该改动未解决 7050，可选择保留或回退。

查看 diff：

```bash
git diff -- app/services/publish/channels/toutiao.py
```

## 8. 建议下一步

优先方向：验证是否为 headless/环境风控。

建议按顺序尝试：

1. 用 headful Chromium + xvfb 跑同样页面自动保存链路。
2. 设置真实 Windows Chrome UA、viewport、locale、timezone。
3. 隐藏 webdriver 和常见 Playwright 指纹。
4. 不直接 API，优先走页面 DOM 输入 + 官方自动保存。
5. 对比真实 PC 自动保存请求 payload 与服务器 Playwright 自动保存 payload。
6. 如仍失败，考虑让 autowz 只生成内容，头条端改成半自动/浏览器插件方案。

## 10. 决定性结论(2026-06-24 复测,根因已锁定)

本轮用 **headful Chromium + xvfb + 反指纹**(`navigator.webdriver=undefined`、真实 Windows Chrome 148 UA、1920x1080、zh-CN、Asia/Shanghai)重跑,并**抓取了头条前端自己用 webmssdk 生成的真实保存请求**做对比。结论如下:

### 逐一排除的假说

| 假说 | 实验 | 结果 |
|------|------|------|
| headless 环境风控 | headful + xvfb + 反指纹 | ❌ 仍 7050(`navigator.webdriver` 已为 None) |
| 缺 msToken / a_bogus 签名 | 抓取前端真实自动保存请求(带正确签名) | ❌ 仍 7050 |
| payload 字段不全 | 用头条**官方前端 JS 自己拼的 POST**(字段比我们更全,含 `title_id`) | ❌ 仍 7050 |
| `article_ad_type` 错误 | 读默认值=3 后提交 | ❌ 仍 7050 |
| 测试内容/标题被判垃圾 | 真实文章(341/342)与测试标题均失败 | ❌ 内容无关 |

> 关键证据:连**头条官方前端 JS 亲自发出、带合法 msToken/a_bogus 签名**的保存请求,在本服务器环境下也返回 7050。会话有效(发布页正常加载、能读 `article/new`、能生成签名),唯一异常变量是**请求来源的网络环境**。

### 根因:服务端基于 IP/网络环境的写操作风控

- 本服务器两条出口 IP 均为**海外数据中心(IDC)**:
  - 代理出口 `35.208.86.96` → 美国 GCP 机房(Council Bluffs, Iowa)
  - 直连出口 `140.238.201.162` → 澳大利亚 Oracle 机房(Sydney)
- **读操作放行**(草稿列表 / article/new / edit 详情均 200),**写操作(保存草稿)被静默拒为 7050"保存失败"**。
- 真实 PC(用户国内住宅 IP)保存 `yun` 草稿成功(§4)。读写差异 + 海外 IDC IP = 头条对异地/机房 IP 的发布写操作做了风控。

**这不是 autowz 侧能靠改请求构造(headless/签名/payload/指纹)解决的问题**,且项目第一纲领明确"走质量路线、不走规避路线",不应去做住宅 IP 伪装等规避检测的事。

### 正确方向(对应 §8 第 6 条):头条改半自动

autowz 负责生成 + 质量闸 + 微信/百家自动草稿;头条端改为**在用户国内住宅 IP 环境运行的保存器**或**导出人工保存**。具体方案见与用户的本轮对话。

## 9. 常用验证命令

查看最近头条发布记录：

```bash
.venv/bin/python - <<'PY'
from app.db.engine import get_db_session
from app.db.models import Article, WechatPublishRecord
from sqlalchemy import desc
with get_db_session() as s:
    for a in s.query(Article).order_by(desc(Article.id)).limit(5):
        print(a.id, a.status, a.title)
        for r in s.query(WechatPublishRecord).filter_by(article_id=a.id).all():
            print(' ', r.publish_status, r.draft_media_id, r.raw_response)
PY
```

只读查看头条草稿箱：

```bash
.venv/bin/python - <<'PY'
import asyncio, json
from playwright.async_api import async_playwright
async def main():
    async with async_playwright() as p:
        browser = await p.chromium.launch(headless=True)
        context = await browser.new_context(storage_state='app/services/publish/cookies/toutiao_state.json')
        page = await context.new_page()
        await page.goto('https://mp.toutiao.com/profile_v4/manage/draft', wait_until='domcontentloaded', timeout=60000)
        await page.wait_for_timeout(10000)
        data = await page.evaluate('''async ()=>{
            const r=await fetch('/mp/agw/creator_center/draft_list?type=0&count=50&app_id=1231',{credentials:'include'});
            return await r.json();
        }''')
        print(json.dumps(data, ensure_ascii=False, indent=2))
        await context.close(); await browser.close()
asyncio.run(main())
PY
```
